import { styled } from "../../config/stitches";

export const HeaderContainer = styled("div", {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
});

export const ButtonLoading = styled("h1", {
  display: "flex",
  justifyContent: "space-between",
});